//Examples:

void println()
void println(boolean x)
void println(char x)
void println(char[] x)
void println(double x)
void println(float x)
void println(int x)
void println(long x)
void println(Object x)
void println(String x)

//Two things that are not a part of a method’s signature:

1 - The method’s return type:
    You can’t code two methods with the same name
    and parameters but with different return types.

2 - The names of the parameters:
    All that matters to the method signature are
    the types of the parameters and the order in which they appear. Thus the
    following two methods have the same signature:

    double someMethodOfMine(double x, boolean y)
    double someMethodOfMine(double param1, boolean param2)

//Note: Signatures are the same, but parameters aren't.